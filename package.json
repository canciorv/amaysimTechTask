{
  "name": "amaysimtechtask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "testHeaded": "npx cypress run --headed --env password=secret",
    "testHeadless": "npx cypress run --headed --env password=secret",
    "testChrome": "npx cypress run --browser chrome --env password=secret",
    "testElectron": "npx cypress run --browser electron --env password=secret"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.4",
    "cypress-iframe": "^1.0.1"
  }
}
